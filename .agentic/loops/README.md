# Loop Contract

All loops declare trigger, objective, scope, agents, tools/permissions, states, gates, retries, stop/escalation, retained outputs, budget, and human-only boundaries. Common rules: least privilege; no real trading; evidence at each gate; diagnose after roughly three same-cause failures; never consume unbounded compute or paid services without user authorization.
