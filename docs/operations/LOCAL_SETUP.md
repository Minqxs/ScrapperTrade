# Local setup

Run `scripts/setup.ps1`, then `scripts/doctor.ps1`. Start with `scripts/start.ps1`; the API binds to loopback and the UI uses the local API. Secrets, media, transcripts, and runtime databases must stay outside Git. Optional FFmpeg, Ollama, OpenAI API, and MT5 dependencies are not required for simulator mode.

